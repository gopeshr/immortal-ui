export function Footer() {
  return (
    <footer className="border-t border-border px-12 py-12 flex justify-between items-center text-muted text-[9px] tracking-[2px] uppercase">
      <div>&copy; {new Date().getFullYear()} Immortal &mdash; Open Source</div>
      <div>Live Forever in Memory</div>
    </footer>
  );
}
